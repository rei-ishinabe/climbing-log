<p><%= @resource.email %>さん</p>

<p>ログインに複数回失敗した履歴があったため、セキュリティ保護のため、クラログアカウントがロックされました。</p>

<p>ロックを解除するには以下のリンクをクリックしてください。</p>

<p><%= link_to 'アカウントのロックを解除する', unlock_url(@resource, unlock_token: @token) %></p>
